import React from 'react';

const GlobalLoading = () => {
    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-gray-800 bg-opacity-75 text-white text-3xl font-semibold">
            Global Loading...
        </div>
    );
};

export default GlobalLoading;